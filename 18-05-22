import cv2 as cv
import numpy as np

img('//', 1)

mtz = cv.array([1,0,-1],
               [2,0,-2],
               [1,0,-1])

cv.imshow('marcoc', resultado)
cv.imshow('marco' img)
cv.waitKey(0)
cv.destroyAllWindows()
